setwd("/Users/imiu/Desktop/Coursera/Data Scientist Certification Classes/4.Exploratory Data Analysis/EDA/ExData_Project2/")
options(prompt="R:EDA_P2> ")

library(utils)
library(base)
library(gRbase)
library(graphics)
library(grDevices)
library(ggplot2)
.libPaths() # get library location
library()   # see all packages installed
search()    # see packages currently loaded
getwd()
options(width=200)

cat("\nIonel, welcome back to R! Today is", date(), "\n")
cat("\nR is ready for you!\n")
cat("\nWorking director is: ", getwd(), "\n")
cat("\nPackages currently loaded are:\n", search(), "\n")

cat("\n                    ======================================", "\n")
cat("\n                      Exploarty Data Analysis - Project 2" , "\n")
cat("\n                    ======================================", "\n")


